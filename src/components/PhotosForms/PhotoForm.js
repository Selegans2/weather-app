import React from 'react';
import PropTypes from 'prop-types';
import PhotoStyle from "./Photo_Style.module.scss";

const PhotoForm = props => {
    return (
        <div>
            <div>
                {/* <img src={`${process.env.PUBLIC_URL}/assets/fondo.jpg`}  alt=""/> */}
            </div>
        </div>
    );
};

PhotoForm.propTypes = {
    
};

export default PhotoForm;